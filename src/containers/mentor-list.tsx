import React from 'react';
import MentorPageButton from '../components/mentor-page-button';

const keywords: string[] = ['백엔드', '프론트엔드'];

function MentorList() {
  return (
    <div>
      <h1>취업</h1>
    </div>
  );
}

export default MentorList;
